/**
 * Alias entry for the main application shell — stays in sync automatically.
 * Prefer importing from `./App` in new code; this file exists for older references or tooling.
 */
export { default } from './App';
